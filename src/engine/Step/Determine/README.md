# Determine

`Determine` is not an active Nodus 0.5 module group for now.

The previous implementation represented bounded option selection. That responsibility currently fits better under qualification/classification, including a future classifier that may select one planner from several available planners.

The old contract is preserved under `src/engine/Deprecated/Determine/` while this boundary remains intentionally open.
