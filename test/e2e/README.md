# E2E

Полный Nodus + реальная модель + проект. Эти тесты дорогие и не входят в обычный `npm test`.

Канонический ручной прогон сейчас:

```bash
npm run dev -- nodus.config.json --clear-cache --clear-logs --scan --scenario=status
```
