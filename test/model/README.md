# Model tests

Тесты с реальной моделью. Они используют тот же `test/framework`, что и integration-сценарии, но подменяют scripted adapter на реальный `ModelAdapter`.

Запуск намеренно последовательный: `npm run test:model`.
