Nodus v0.1 — plan/final-result patch

Apply this archive OVER the previous logging + context-optimization version.

Changed files:
- src/agent/Runtime/AgentRuntime.ts
  - plan is limited to 2 tool batches
  - plan with status=continue but no action auto-transitions to understand
  - operation transitions are logged with reasons
  - completed result keeps the full final answer (with backward-compatible fallback to observations)
- src/model/Result/OperationResult.ts
  - adds finalAnswer?: string
- src/model/Controller/ModelController.ts
  - parses/logs finalAnswer
  - response protocol distinguishes execution message from final user answer
- src/model/Profile/PromptRegistry.ts
  - plan explicitly stops evidence gathering after <=2 rounds
  - understand prefers completing from existing toolContext
  - completed operations return full text in finalAnswer

Expected flow for the architecture-analysis smoke task:
plan -> tools (<=5) -> plan -> tools (<=5) -> understand -> completed
or faster if plan transitions earlier.

New useful log events:
- operation-transition { from, to, reason }
- plan-budget-exhausted { toolBatches, limit }
